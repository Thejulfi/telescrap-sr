[package]
name = "telescrap-sr"
version = "2.0.2"
edition = "2024"

[dependencies]
scanner = { path = "crates/scanner" }
parser = { path = "crates/parser" }
telegram-notifier = { path = "crates/telegram-notifier" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
dotenv = "0.15"