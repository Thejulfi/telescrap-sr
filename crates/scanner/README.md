# Scanner

This crate is responsible for the scanning process, which consists in periodically checking the ticketing website and filtering the relevant information. It uses the `parser` crate to extract the relevant information from the HTML content.